# Changelog

## [Unreleased]

### Added

- Added DAG unit tests (`test/dag.test.ts`) covering dependency graph building, topological sorting, cycle detection (Kahn's algorithm), and execution wave computation
- Added schema validation tests (`test/schema.test.ts`) for swarm task and agent configuration schemas
