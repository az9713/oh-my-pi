# Changelog

## [Unreleased]

### Added

- Added `.env` file loading from omp config directories (`~/.omp/agent/.env` for user-level, `.omp/.env` for project-level) with higher priority than generic `.env` files
